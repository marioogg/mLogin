security {
    secret-key = ""
}

redis {
    host = "localhost"
    port = "6379"
    password = ""
}

sql {
    host = "localhost"
    port = 3306
    database = "mlogin"
    username = "root"
    password = ""
}

rate-limit {
    max-attempts = 5
    attempt-window-seconds = 60
    base-block-seconds = 30
    max-block-seconds = 900
    strike-window-seconds = 3600
}